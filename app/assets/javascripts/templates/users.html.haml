.device-header
  %a{"class" => "device-header-backbtn", "href" =>  "#/"}
    / %img{src: "/images/close-btn.png"}
  %a{"class" => "device-header-logo", "href" =>  "#/"}
    %img{src: "/images/rotary-logo.png"}
  .title-container
    %h2
      {{ "MEMBERS.title" | translate }}

%section.popup
  .resource-container
    .header
      %a{"class" => "page-logo", "href" =>  "#/"}
        %img{src: "/images/rotary-logo.png"}
      %h1.tagline
        {{ "MEMBERS.title" | translate }}

    .users-container
      %h3
        {{ "MEMBERS.president" | translate }}
      .user-item.col-lg-4.col-lg-offset-4.col-md-4.col-md-offset-4.col-sm-6.col-sm-offset-3.col-xs-10.col-xs-offset-1{"ng-repeat" => "user in users.users | filter: {role: 'president'} "}
        .image{"ng-href" => "#/users/{{ user.id }}"}
          %img(src="{{user.image}}")
        %p.name
          {{user.name}}
        %div{"class" => "flag {{ user.nationality}}"}  
        %div{ "ng-if" => "user_logged_in"}
          .btn-container
            %a.to-page{"ng-href" => "#/users/{{ user.id }}"}
            .to-popup{"ng-click" => "showDetails = ! showDetails"}
              .hide-container{"ng-show" => "showDetails"}
                %img{:src => "/images/icons/triangle_hover.png"}
                %p
                  {{user.phone}}
                %p
                  {{user.email}}
                %p
                  {{user.address}}

        .clear
      .clear
    .users-container
      %h3
        {{ "MEMBERS.сommitties" | translate }}
      .user-item.col-lg-4.col-lg-offset-2.col-md-4.col-md-offset-2.col-sm-5.col-sm-offset-1.col-xs-10.col-xs-offset-1{"ng-repeat" => "user in users.users | filter: {role: 'committies'} ", "ng-click" => "showDetails = ! showDetails"}
        .image{"ng-href" => "#/users/{{ user.id }}"}
          %img(src="{{user.image}}")
        %p.name
          {{user.name}}
        %div{"class" => "flag {{ user.nationality}}"} 
        %div{ "ng-if" => "user_logged_in"}
          .btn-container
            %a.to-page{"ng-href" => "#/users/{{ user.id }}"}
            .to-popup{"ng-click" => "showDetails = ! showDetails"}
              .hide-container{"ng-show" => "showDetails"}
                %img{:src => "/images/icons/triangle_hover.png"}
                %p
                  {{user.phone}}
                %p
                  {{user.email}}
                %p
                  {{user.address}}
        .clear
      .clear
    .users-container
      %h3
        {{ "MEMBERS.regular" | translate }}
      .user-item{"ng-repeat" => "user in users.users | filter: {role: 'regular'} ", "ng-click" => "showDetails = ! showDetails"}
        .image{"ng-href" => "#/users/{{ user.id }}"}
          %img(src="{{user.image}}")
        %p.name
          {{user.name}}
        %div{"class" => "flag {{ user.nationality}}"} 
        %div{ "ng-if" => "user_logged_in"}
          .btn-container
            %a.to-page{"ng-href" => "#/users/{{ user.id }}"}
            .to-popup{"ng-click" => "showDetails = ! showDetails"}
              .hide-container{"ng-show" => "showDetails"}
                %img{:src => "/images/icons/triangle_hover.png"}
                %p
                  {{user.phone}}
                %p
                  {{user.email}}
                %p
                  {{user.address}}
        .clear
      .clear

%a.fadeout{"ng-href" => "#/"}
  .close-btn